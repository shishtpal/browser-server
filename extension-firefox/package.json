{
  "name": "@browser-server/extension-firefox",
  "version": "0.1.0",
  "private": true,
  "description": "Firefox wrapper for the Browser Server extension.",
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --mode background && vite build --mode content",
    "type-check": "vue-tsc --noEmit"
  },
  "dependencies": {
    "@browser-server/extension-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.6.1",
    "@types/webextension-polyfill": "^0.12.5",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vue-tsc": "^3.0.8"
  },
  "packageManager": "pnpm@11.6.0"
}
