{
  "name": "@browser-server/extension",
  "version": "0.1.0",
  "private": true,
  "description": "Browser Server extension built with Vite, TypeScript, Vue 3, and Tailwind CSS v4.",
  "type": "module",
  "scripts": {
    "dev": "vite build && vite build --mode content && vite build --watch",
    "build": "vite build && vite build --mode content",
    "type-check": "vue-tsc --noEmit"
  },
  "dependencies": {
    "@browser-server/extension-core": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "@types/node": "^24.6.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vue-tsc": "^3.0.8"
  },
  "packageManager": "pnpm@11.6.0"
}
