{
  "type": "object",
  "properties": {
    "context": {"type": "string", "description": "One or two lines explaining why clarification is needed."},
    "questions": {
      "type": "array",
      "minItems": 1,
      "maxItems": 5,
      "items": {
        "type": "object",
        "properties": {
          "id": {"type": "string"},
          "prompt": {"type": "string"},
          "kind": {"type": "string", "enum": ["text", "choice", "multi_choice", "confirm"]},
          "options": {"type": "array", "items": {"type": "string"}, "maxItems": 20},
          "default": {"type": "string"},
          "required": {"type": "boolean"}
        },
        "required": ["prompt"],
        "additionalProperties": false
      }
    }
  },
  "required": ["questions"],
  "additionalProperties": false
}
