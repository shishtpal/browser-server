{
    "type": "object",
    "properties": {
        "path": {
            "type": "string",
            "description": "Directory path to generate tree for; defaults to the server working directory"
        },
        "max_depth": {
            "type": "integer",
            "description": "Maximum depth to recurse (1-20, default 5)",
            "minimum": 1,
            "maximum": 20
        },
        "ignore_patterns": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Additional file/directory patterns to ignore (supports glob patterns like *.log). .git and node_modules are always ignored."
        }
    },
    "additionalProperties": false
}